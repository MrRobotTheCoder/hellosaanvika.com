pipeline {
    agent any

    stages {
        stage('Checkout Verification') {
            steps {
                echo 'Repository checkout successful'
                sh 'git --version'
            }
        }
    }
}

